export interface NewsArticleDateProps {
  /**
   * The text in the hint
   */
  text: string;
  /**
   * Does the component need a deep margin below it? This is based on if there is an image or not attached to the news article
   */
  withMargin: boolean;
}
