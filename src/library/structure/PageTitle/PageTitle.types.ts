import React from 'react';

export interface PageTitleProps {
  /**
   * Any unique class names to apply to the page container
   */
  classes?: string;

  /**
   * The PageTitle contents
   */
  children?: React.ReactNode;
}
